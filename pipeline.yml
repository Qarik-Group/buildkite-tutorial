steps:
steps:
  - label: "{{matrix}} build"
    command: "GOOS={{matrix}} go build"
    env:
      os: "{{matrix}}"
    matrix:
      - "$$BUILDKITE_PIPELINE_ID"
      - "Linux"
      - "Windows"
notify:
  - email: "stephanie@buildkite.com"
    if: build.state == "passed"
